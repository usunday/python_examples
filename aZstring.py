import string

lower=list(string.ascii_lowercase)
upper=list(string.ascii_uppercase)

def long_repeat(line):
    line_list=list(line)
    for char in line :
        for low in lower :
            if(char == low) : 
                
                
        
    return 0
